{
  "name": "ags-agents",
  "version": "0.1.0",
  "private": true,
  "description": "AGS Agent Factory - building AI agents in public",
  "author": "Tomasz Nawrocki <hello@authenticgrowthsystems.com>",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "dotenv": "^16.4.0"
  }
}
